package de.elvirakraft.docmanagement.security;

import de.elvirakraft.docmanagement.entities.User;

import java.util.Map;

/**
 * Wrapper class for the Spring User object.
 */
public class UserWrapper extends User {
    /*public UserWrapper(Long id, String name, String surname, String company, String email, String password, boolean isDeleted, UserRole userRole) {
        super(id, name, surname, company, email, password, isDeleted, userRole);
    }

    private Integer userId;
    private Map<>
*/}
